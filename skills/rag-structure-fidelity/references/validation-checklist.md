# 校验清单

## 必查项

- 是否只把 `md` 当正文主输入
- `.lakebook` 是否仅作目录对照
- 扫描件 / 图片型 PDF 是否被阻断
- 表格是否按整表判定
- 列表是否按整块判定
- 图片是否仅保留引用和位置
- 关键规则区块是否可单独阻断
- 是否只生成候选证据，不自动 RC
- 是否不直接组下游包
- 是否无敏感信息泄漏

## 失败条件

- 自动把候选映射当正式真值
- 自动生成 RC
- 跨空间混入
- 目录错位却仍然直接放行
- 正文失真却未标阻断

