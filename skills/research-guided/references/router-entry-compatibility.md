# Entry compatibility

This package preserves the original research implementation, schemas, evidence ledgers, strong-gate default, approved light exception, one content reinforcement round and failure review. The directory/name changed; historical evidence remains historical.

## Executor selection

The new research sibling is the generic router. Direct guided entry defaults to Claude first. If the user explicitly selects Codex, Codex performs retrieval directly with the same outline, source verification, coverage, reinforcement and final review; do not first invoke Claude or call this a Claude-failure fallback. This approved entry exception takes precedence over legacy statements requiring Claude for standard/deep research.

## Preserved profiles

Claude uses all existing preflight, registry, schema and adapter checks unchanged. Codex uses current host retrieval tools and the router host-receipt contract; do not invent Claude sessions, MCP visibility, canary evidence or native Schema enforcement. Original Claude packets still pass their original validators; the new host receipt does not pretend to be a Claude packet. No research depth downgrade is automatic.

Claude unavailability does not automatically authorize Codex: use an actual user selection or applicable prior authorization, otherwise pause and ask. A user-selected Codex task is already authorized by that choice.

Read ../research/references/guided-handoff.md through the sibling router when returning evidence. State progress and actual executor honestly; no external call or installation is authorized by copying this Skill.
