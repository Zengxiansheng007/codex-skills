# 验证与追踪回顾

版本：1.0.0；日期：2026-09-05。测试数量按实际阶段区分，不把安装、发布或业务验收混成一个“整体通过”。

## 已有本地证据

| 验证层 | 结果 | 边界 |
|---|---|---|
| 通用候选完整回归 | 436 通过 | 不访问项目业务页面 |
| 项目 adapter 完整回归 | 119 通过 | 含真实 JetBrains helper 合成正/负 canary，不等于业务运行 |
| 安装后通用回归 | 436 通过 | 安装后读回与敏感扫描均通过 |
| A/B qualification | 两项通过 | submit=0，序列不分配，未触发城市恢复 |
| successor/CAS/部署读回 | 通过 | 旧失败及历史结果未改写 |
| 人工业务确认 | 用户确认通过；本地记录支持业务成功 | 不是外部进程证据，也不自动满足原计划的顺序和次数 |

发布副本将再次执行通用测试、敏感扫描及文件读回。发布验证结果以本次发布索引为准，不复用上述历史次数冒充新的执行结果。

## 需求到证据映射

| 需求 | 实现/验证 | 当前状态 |
|---|---|---|
| FR-013 / AC-013、015 | finalization 生命周期、真实 hook 分发与 helper canary | 实现与离线测试已验证 |
| FR-014 / AC-014 | 内容寻址对象、原子 commit、故障注入与崩溃恢复 | 实现与离线测试已验证 |
| FR-015 / AC-013 | 稳定错误码、failure receipt、非空 longrepr/TeamCity details | 实现与离线测试已验证 |
| FR-016 / AC-016 | 外部 Acceptance 校验拒绝非零 exit、缺 receipt、post-run 补封存 | 校验器已验证；实际人工验收闭包尚缺进程证据 |
| FR-017 / AC-017、018 | PyCharm A/B、每 node 一次 submit 与业务后置验证 | 用户业务确认通过；实际 B、B、A 的顺序/次数偏差待验收评审 |
| FR-018 / AC-019 | 2.2 配置、新 Schema、旧版本只读、部署身份 | 候选与正式部署已验证 |
| 城市目录恢复授权 | 目录失败后列表重入、单次预算、填写/提交后禁止、异常停止 | 离线回归通过；真实故障分支未触发 |

## 文档纠偏

原 ST-013 文档保留其历史 workspace 状态；本发布回顾补充已经完成的安装、qualification、activation，以及后续用户人工确认，不修改旧需求锚点与历史结果。根目录的当前状态不应再写“真实 A/B 尚未执行”，应写“业务人工确认通过，最终机器验收未关闭”。

尚未形成可接受的外部 ProcessEvidence/AcceptanceResult，且 AC-018 的顺序、次数条件与实际记录不一致；不能用“用户通过”覆盖这些事实，也不能为了补证自动再次创建数据。项目整体维持 functional-accepted / repair-needed。GitHub 发布可交付已验证的通用代码和这一限制说明，不宣称最终 CompletionEvaluator 已通过。

## 环境与排障记录

早期测试中的版本断言、快照 lineage 字段、helper 路径配置及子进程导入环境问题已经纠正，失败 XML 保留。部署 trace-config 测试限定临时项目根，避免扫描其他 Temp 目录的竞态。发布测试显式设置 PYTHONPATH 指向实际发布副本，防止误测已安装的其他版本。
