# UI-Test 修复回顾调整包

本包是 2026-09-03 的 `development-system` 合规后继文档，包含完整 PRD、As-Built 架构、开发计划、As-Tested 测试方案、逐项 RTM、Phase/Story、变更影响、规范 Envelope 与可公开证据摘要。

权威入口：

- `current-artifacts.json`：当前版本和 supersedes 关系；
- `governance/ui-test-repair-requirement-anchor-v0.7.json`：需求锚点；
- `review/final-landed-review.json`：最终 no-drift 复核；
- `evidence/completion-result.json`：CompletionEvaluator 结果；
- `document-manifests/`：由 UTF-8 实际内容和源提交生成的发布 manifest。

公开边界：不包含项目适配器、正式项目资产、私有 URL、凭据、请求载荷或原始运行证据。
